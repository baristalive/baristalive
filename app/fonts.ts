import { Fredoka, Instrument_Sans, Inter, Saira } from "next/font/google";

export const inter = Inter({
  subsets: ["latin"],
  display: "swap",
});

export const fredoka = Fredoka({
  subsets: ["latin", "latin-ext"],
  display: "swap",
});
